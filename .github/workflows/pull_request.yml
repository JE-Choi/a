name: pr 머지되면 실행

run-name: pr 머지되면 실행 | ${{ github.event.pull_request.title || github.event_name }}

on:
  pull_request:
    types:
      - closed
    branches:
      - 'main'
  workflow_dispatch:
  
jobs:
  job1:
    if: |
      github.event_name == 'workflow_dispatch' || 
      (github.event.pull_request.merged == true && github.base_ref == 'main'  && github.head_ref == 'release')
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: exit 1
  job2:
    needs: [job1]
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo success
  
